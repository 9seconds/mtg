---

language: go
sudo: false
dist: trusty

go:
  - "1.9.x"
  - "1.10.x"
  - master

script:
  - make prepare
  - make all
  - make lint
  - make critic
  - make test

cache:
  directories:
    - vendor
